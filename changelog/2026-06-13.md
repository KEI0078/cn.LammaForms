# 2026-06-13 开发日志（v2.4.2 → v2.4.3 完整工作记录）

> v2.4.2/v2.4.3 两个版本在一日内连续开发，修了原版代码（原作者 v2.3 起）遗留的 3 个长期问题和 v2.4.0 引入的 4 个隐藏 bug。

---

## 修改汇总

### 🔴 核心 Bug 修复

| 编号 | 问题 | 症状 | 修复 |
|------|------|------|------|
| B-7（长期） | **b9601 速度输出格式变更——正则不匹配** | 批量测速/CPU 多线程测试全 0.0 t/s | `ParseSpeedLine` + `ParseSpeedFromOutput` 正则从 `prompt eval time = ... ms / ... tokens` 改为 `[ Prompt: X t/s \| Generation: Y t/s ]`，直接取 speed 值 |
| B-8（v2.4.0） | **批量测速 ctx-size 被 baseArgs 覆盖** | 测 4K 实际跑的是 32K，MTP 显存爆 | `-c {contextSize}` 移到 `{baseArgs}` 后面 |
| B-9（v2.4.0） | **CPU 多线程测试缺漏 --model-draft** | 多线程测试 MTP 不生效，全 0.0 t/s | 补上 mtpFile/mtpArg 拼接逻辑 |
| B-10（v2.4.0） | **批量测速缺漏 --model-draft**（跟 B-9 同根因） | 批量测速 MTP 不生效 | B-1（6/12 修复）已修，但 git checkout 回退后重新应用 |
| B-11（v2.3 起） | **右上角 Token 统计始终为 0** | tb_tokens 只显示服务器 Task 且全 0 | 批量测速每次出结果后更新 `_stat*` 变量 + `tb_tokens.AppendText` + `UpdateTokenStatusPanel()` |
| B-12（v2.3 起） | **CPU 多线程测试右上角无显示** | tb_tokens 为空 | 添加 `tb_tokens.Clear()` + 每次结果追加 |
| B-13（v2.3 起） | **并发压测（高级工具B）Token 统计为 0 + 测试结果页空白** | 底部面板 0、tb_tokens 0、测试结果 Tab 无内容 | 新增 `ExtractPromptTokens`/`ExtractCompletionTokens`，总结时更新 `_stat*` + `UpdateTokenStatusPanel()` + `tb_tokens.Clear()` 写摘要 |

### 🛠️ 功能改进

| 编号 | 改进 | 说明 |
|------|------|------|
| I-1 | **高级工具全量输出到测试结果页** | `AppendRunningLog()` 补全 4 个按钮（基准测试/模板分析/并发压测/查看汇总） |
| I-2 | **llama-template-analysis 参数修正** | 从错误的 `-m "model.gguf"` 改为正确的 `--all` |
| I-3 | **窗口标题精简** | 从超长标题改为 `llama.cpp本地启动器 / 原作者B站@贤小二 / 修改者github@KEI0078` |
| I-4 | **并发压测统计面板更新** | 使用总 token 数/总耗时算平均速度写入 `_stat*` |

### 📦 版本管理

- 新建版本目录：`cn.Lamma-windows v2.4.2/`
- Git commit：`a98e4f6` — `v2.4.2 - 修批量测速/多线程速度解析、统计面板、并发压测统计、模板分析`

---

## 各功能测试结果（v2.4.3 最终验证）

### 批量上下文测速（MTP 开启）

| Context | Prompt | Generation |
|---------|--------|-----------|
| 4K | 255.1 t/s | 105.4 t/s |
| 8K | 250.5 t/s | 125.6 t/s |
| 128K | 256.8 t/s | 111.4 t/s |
| 192K | 253.8 t/s | 114.9 t/s |

**MTP 加速效果**：生成速度从 58.7 t/s（无 MTP）→ 平均 ~115 t/s，**约 2 倍提升**。

### CPU 多线程测试（8K ctx, MTP 开启）

| 线程数 | Prompt | Generation |
|--------|--------|-----------|
| 6 | 249.2 t/s | 99.9 t/s |
| 7 | 241.0 t/s | **129.3 t/s** 🏆 |
| 8 | 257.0 t/s | 111.7 t/s |
| 9 | 220.4 t/s | 80.4 t/s |
| 10 | 209.3 t/s | 102.2 t/s |
| 11 | 216.1 t/s | 107.8 t/s |
| 12 | 222.1 t/s | 116.5 t/s |

**最佳线程数：7**，生成速度 129.3 t/s。

### llama-bench 基准（无 MTP）

| 测试项 | 速度 |
|--------|------|
| Prompt 512 | 3606 t/s |
| Generation 128 | 58.7 t/s |

### 并发压测（8 并发）

| 指标 | 数值 |
|------|------|
| 成功数 | 8/8 |
| 累计 tokens | ~4,300 |
| 平均吞吐 | ~33 t/s |

---

## 硬件配置

- **GPU**: NVIDIA GeForce RTX 4070 SUPER, 12 GB VRAM
- **CUDA**: 13.3
- **llama.cpp**: b9601 (4c6595503)
- **模型**: unsloth/gemma-4-12B-it-qat-GGUF:UD-Q4_K_XL
- **MTP 模型**: mtp-gemma-4-12B-it.gguf (253 MB)

---

## 部署路径

```
编译 → project/build-output/
  ↓ 阶段一（测试）
cn.LammaForms/cn.LammaForms/cn.Lamma-windows v2.4.2/
  ↓ 阶段二（正式 - 待主人手动复制）
E:\llama.cpp\cn.Lamma-windows v2.4.1\
```

正式版由主人自行复制替换。

---

## Git 版本

```bash
cd E:/llama.cpp/cn.LammaForms
git log --oneline -3
# a98e4f6 v2.4.2 - 修批量测速/多线程速度解析、统计面板、并发压测统计、模板分析
```

**SSH 推送**（主人偏好）：
```bash
cd E:/llama.cpp/cn.LammaForms
git push origin master
```
